export default function HomePage() {
  return (
    <main style={{ padding: 24 }}>
      <h1 style={{ marginBottom: 8 }}>RedReader</h1>
      <p>Skeleton repo. Next step: scaffold Next.js + Tailwind, then build Library + Reader pages.</p>
    </main>
  );
}
